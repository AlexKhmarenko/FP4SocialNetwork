name: Coveralls

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test_and_report:
    name: Test and Report
    runs-on: ubuntu-latest

    steps:
#      - name: Checkout
#        uses: actions/checkout@v2

#      - name: Setup Java
#        uses: actions/setup-java@v2
#        with:
#          java-version: '17'

#      - name: Build with Maven
#        run: mvn -f social_network_api/pom.xml -B package --file pom.xml

      - name: Run Tests with Jacoco
        run: mvn -f social_network_api/ test jacoco:report coveralls:report